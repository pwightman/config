set fuopt=maxvert,maxhorz
if has("gui_running")
	colorscheme obsidian
endif
if has("gui_running")
	set guioptions=egmrt
endif
set columns=150 lines=65
set transparency=3
set guifont=Source\ Code\ Pro:h13

